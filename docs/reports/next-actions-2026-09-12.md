# 下一步任务清单 — 2026-09-12

> 依据：`docs/reports/repo-status-2026-09-12.html`（仓库状态实测分析）
> 基线提交：`29c01bd`（main，领先 origin/main 117 提交）
> 排列原则：**先固化已完成的工作，再产出新信息，最后才动新功能。**

---

## 执行记录（2026-09-12）

| # | 任务 | 状态 | 结果 |
|---|---|---|---|
| 1 | 提交并收编试玩存档隔离功能 | ✅ 完成 | 提交 `9603469`；新增 `playtest_id_isolation_test.gd`（115 断言）并注册；套件 6876/0 |
| 2 | 推送提交到 origin/main | ✅ 完成 | 118 个提交已推送（46 MB 包触发 HTTP 408，改为分 8 段推送成功）；`origin/main == main == 9603469` |
| 3 | 试玩前独立复核动作与接触验收结论 | ✅ 完成 | 见 `production/qa/art-review-2026-09-12-independent-verification.md`：问题 A／C确证修复，问题 B 推进到 16 工位矩阵但像素级判定待补 |
| 6 | 让 SaveLoad 存档根目录可注入 | ✅ 完成 | 新增 `set_save_root_override()` seam；迁移 4 个依赖真实落盘的测试；新增 `save_root_override_test.gd`（32 断言）。**受限环境与常规环境均 6908/0**，原 16 项假阳性消失 |
| 9 | 归档根目录遗留的 V31_R7_P3_EVIDENCE.md | ✅ 完成 | 迁入 `tests/evidence/`，与 R6_P1/R6_P3/R7_P1/R7_P2 四个同类文档归位；根目录已无遗留 |

**仍待处理**

| # | 任务 | 阻塞原因 |
|---|---|---|
| 4 | 执行首轮 5 人真实玩家试玩 | **需要真人受试者**，无法代劳。前置（试玩存档隔离）已完成，方案可直接执行 |
| 5 | 清理 worktree 与遗留分支 | 属**破坏性操作**，需你确认后再执行 |
| 7 | 收编或忽略 293 个未跟踪文件 | 需你决定 `.import` / `.uid` 的版本控制策略 |
| 8 | 回填文档与实现的偏差 | 未开始 |
| 10 | CI 增加导出版构建验证 | 未开始 |
| 11 | 启动 Vertical Slice 系统 | 等试玩结论 |

---

## P0 — 阻塞级：先把已完成的工作固化下来

### 1. 提交并收编试玩存档隔离功能

三个文件当前未提交（+214 / −30）：`src/main.gd`、`src/systems/save_load.gd`、`tests/integration/save_load/file_io_version_test.gd`。

内容是「试玩存档隔离」：`--playtest-id` / `--resume` 启动参数、`saves/playtests/<ID>/` 独立目录、存档原子替换、路径穿越校验。

- [ ] 提交这组改动
- [ ] 补专项测试文件：路径穿越（`..` `/` `\` `:`）、ID 超长（>64）、空值、大小写归一化、两玩家存档互不覆盖
- [ ] 注册进 `tests/headless_runner.gd` 的 `TEST_FILES`（漏注册会被 `_check_registry_coverage()` 判失败）
- [ ] 全量套件确认 0 失败

**预期目标**：工作区回到 clean；试玩 ID 隔离有独立断言保护，不再是一次性脚本式改动。

**验收**：`git status` clean；新测试已注册；套件 6761+ 断言 0 失败。

### 2. 推送 117 个提交到 origin/main

所有工作目前仅存在于本机，origin 停在 2026-08-10。**这是当前最大的单点风险。**

- [ ] 确认工作区 clean、套件通过
- [ ] `git push origin main`

**预期目标**：远端与本地一致，本机故障不再等于工作丢失。

**验收**：`git rev-list --count origin/main..main` 输出 `0`。

---

## P1 — 产出新信息：试玩

### 3. 试玩前独立复核动作与接触验收结论

2026-09-09 复评记录了三处未闭合：四 tick 上机过渡覆盖不全、锚点存在 ≠ 四朝向接触全通过、部分录像为自动化业务命令而非真实输入。2026-09-10 提交声称已修复，但**未见独立复评确认**。

- [ ] 四器械 × 允许朝向逐帧接触复核
- [ ] 散客首次上机 / 课程二次换站 / 4-tick 内暂停 / 中途读档 四场景复核
- [ ] 给出独立确认或否决（不能只引用提交信息）

**预期目标**：避免把未验证的结论带入玩家测试，否则试玩反馈会与已知缺陷混淆。

### 4. 执行首轮 5 人真实玩家试玩

方案已就绪：`docs/plans/2026-09-10-first-playtest-plan.md`。**当前唯一能产出新信息的动作。**

- [ ] 用 `--playtest-id` 为每位受试者分配独立存档
- [ ] `--resume` 支持中断续玩
- [ ] 冻结 32° 浅镜头 + 紧凑 HUD + 默认布局，禁止临时增减 NPC / 器械 / 装饰
- [ ] 主持人全程沉默，除按键表外不给引导
- [ ] 记录四大指标：T1 首次指导耗时 / M1 误操作率 / W1 等待感 / R1 续玩意向
- [ ] 后台 30 FPS 录屏 + 麦克风旁白

**预期目标**：5 份观测记录 + 原始录屏；按方案第 6 节规则自动裁决出**唯一一项**美术精修方向（人物动作 / 脚底接触 / 器械材质，三选一，不得同时执行）。

### 5. 清理 worktree 与遗留分支

现状：178 个 worktree、189 条分支、160 条已并入 main、23 条显示未合并。

⚠️ **前提**：那 23 条「未合并分支」经 `git cherry` 判定绝大多数是遗留副本（各只领先 1–2 个提交，main 已领先 200–280），成果早已通过其他分支落地 —— 不要误判为待收编的工作。

- [ ] 先删零独有提交的 5 条：`t_3c527da5`、`t_75a395c0`、`t_aa618b98`、`t_f1010c0a`、`t_b7d89829`
- [ ] 删除已 `prunable` 的 `/private/tmp/p1_baseline`
- [ ] 其余 18 条用 `git cherry main <branch>` 逐条判定后再删
- [ ] 评估 1 条陈旧 stash（`wt/t_c272d32b: evidence pngs pre-rebase`）
- [ ] `git worktree prune` + 清理 `.worktrees/`

**预期目标**：分支数降至个位数，`git branch` 不再把已完成工作误报为「未合并」。

---

## P2 — 消除系统性脆弱性

### 6. 让 SaveLoad 存档根目录可注入

落盘路径硬编码在 OS 用户数据目录，任何受限环境（沙箱、无家目录容器）都会让存档类测试假性失败 —— 本次分析中即出现 16 项假阳性。CI 跑在 ubuntu 上恰好有可写 HOME，脆弱性被长期掩盖。

- [ ] 引入可注入的存档根目录：测试用临时目录，生产用 `user_data_dir`

**预期目标**：套件在任意环境给出同一结果。

**验收**：无写权限环境下与正常环境结果一致。

### 7. 收编或忽略 293 个未跟踪文件

构成：189 个 `.import`、71 个 `.uid`、14 个 py、14 个 png、4 个 sh、1 个 md。

- [ ] 决定 `.import` / `.uid` 策略（`.gitignore` 中 `*.import` 现为注释状态，需明确二选一）
- [ ] 收编或忽略 `tests/evidence/gate/` 与 4 个 `.sh` 脚本

**预期目标**：`git status` 干净可读，不再有近 300 项噪声。

### 8. 回填文档与实现的偏差

四套并行进度表述互相矛盾，逐项修正：

- [ ] `design/gdd/systems-index.md`：#21 Audio 标为 Not Started，实际已完整实现（75 项断言）
- [ ] `production/epics/gym-adventure/`：story-001/002 标 In Progress，实际已交付
- [ ] GDD #13 / #14 / #15 未终审，而对应 16 个 story 早已 Complete 并通过 QA
- [ ] Sprint 5 签核的「缺少 main_scene」条件已不成立（`project.godot` 已有 `run/main_scene`）
- [ ] `production/sprints/` 缺 sprint-4 / 5 与冒险切片文件

**预期目标**：四套表述口径一致，核对真实进度不再需要人工交叉比对。

### 9. 归档根目录遗留的 V31_R7_P3_EVIDENCE.md

- [ ] 移入 `docs/` 下合适位置，从根目录移除
- [ ] 顺带排查是否还有其他同类遗留

**预期目标**：仓库根目录无遗留证据类文档。

### 10. CI 增加导出版构建验证

当前 CI 只跑 headless 测试，无法发现打包问题。

- [ ] 增加 Godot export templates 导出版构建，并作为合并门禁

**预期目标**：CI 覆盖从「测试通过」扩展到「能打出可运行构建」。

---

## P3 — 试玩之后再决定

### 11. 启动 Vertical Slice 系统

尚未开始的 5 个系统：#17 器械信息面板、#18 里程碑、#19 进度解锁、#20 新手引导、#22 设置与无障碍。

⚠️ **刻意排在试玩之后**。玩家反馈很可能改写优先级，甚至改变哪些系统值得做 —— 现在投入容易白做。

- [ ] 依 5 人试玩结论确定取舍与实现顺序
- [ ] 再拆 story

**预期目标**：由真实玩家数据驱动功能取舍，而非在未经验证的假设上投入工作量。

---

## 依赖关系

```
1 收编存档隔离 ──┬──> 2 推送提交
                 └──> 4 试玩 <── 3 验收复核
                              └──> 11 Vertical Slice

独立可并行：5 清理分支 · 6 存档可注入 · 7 未跟踪文件 · 8 文档回填 · 9 归档 · 10 CI 导出
```
