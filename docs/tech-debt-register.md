# Tech Debt Register

Advisory deviations accepted at story-close time. Each entry records what was
accepted, why, and where it came from. Review with `/tech-debt`.

---

## Open

- ~~**2026-07-25** (Story 001: Grid Core — Cell Data Model): `GridSystem extends SimSystem` rather than `GridStateReader`. ADR-0003's Migration Plan states GridSystem should subclass `GridStateReader` "from the start," but that class is not built until Story 006. Resolve when Story 006 inserts the intermediate base class — tracked from `production/epics/grid-system/story-001-grid-core-cell-data.md`~~ → **2026-08-02 已解决 (Story 006)**: `GridSystem` now extends `GridStateReader`, which itself extends `SimSystem` (per ADR-0003's governing hierarchy — NOT RefCounted directly, because GridSystem must keep SimSystem's init machinery through a single chain). GridStateReader is inserted between SimSystem and GridSystem exactly as the ADR's migration plan intended.
- **2026-07-25** (Story 001: Grid Core — Cell Data Model): `SimSystem` exposes `_mark_initialized()` and `_assert_initialized()`, which are not named in ADR-0001's Key Interfaces section. They implement ADR-mandated behaviour ("init() must only be called once"; "every public method must guard against use-before-init") under names the ADR does not specify. Consider a small ADR-0001 clarification recording the verified Godot 4.7.1 override-signature constraint that forced the naming, so the next 11 systems follow the same pattern deliberately — tracked from `production/epics/grid-system/story-001-grid-core-cell-data.md`
- **2026-07-25** (Story 001: Grid Core — Cell Data Model): Untyped `Array` parameters and return types violate the project's mandatory static-typing standard — `get_access_ids() -> Array` should be `Array[int]`; `set_buildable_bulk(cells: Array, ...)` and `get_transformed_cells(footprint: Array, access: Array, ...)` should be `Array[Vector2i]`. The untyped params also force manual `as Vector2i` casts in `get_transformed_cells()`. Low risk, mechanical fix — tracked from `production/epics/grid-system/story-001-grid-core-cell-data.md`
- **2026-07-25** (Story 001: Grid Core — Cell Data Model): `.gitignore:43` contains `!.godot/global_script_class_cache.cfg`, forcing an editor-generated cache file into version control. The file now holds genuine engine output (it previously held hand-written, partly fictional entries), but it will churn on every editor open. Decide whether headless CI genuinely needs it committed, or whether the CI job should run `godot --headless --editor --quit` to regenerate it — tracked from `production/epics/grid-system/story-001-grid-core-cell-data.md`
- **2026-07-31** (Story 002: Solidity Formula and Coordinate Conversion): AC-D2.2 literally requires `push_error()` to fire on out-of-bounds queries, but `grid_solidity_coords_test.gd` only verifies the safe-default return value and the absence of a cross-cell data leak — it does not assert that `push_error()` itself was invoked. The project's custom `tests/headless_runner.gd` SceneTree runner has no GUT-style error-capture harness, so this may not be cheaply fixable. Several upcoming stories in this epic have similar "must push_error()" acceptance criteria (e.g. story-005's AC-C7.2/C7.3/C7.7) — worth deciding once whether to build a lightweight push_error-capture test helper, or formally accept return-value verification as sufficient evidence project-wide — tracked from `production/epics/grid-system/story-002-grid-solidity-coords.md`
- **2026-07-31** (Story 002: Solidity Formula and Coordinate Conversion): ADR-0001 §3's `PlacementInputBridge` example code comments `_system.world_to_grid(screen_pos)` as a single-argument call, implying grid↔world conversion was expected to live on `PlacementSystem` with a one-parameter signature. Story 002 instead placed `world_to_grid(world_pos, cell_size)` on `GridSystem` with a required second parameter (per GDD D.4's cell_size-must-never-be-hardcoded rule). This is very likely the correct evolution — the ADR-0001 snippet predates GDD D.4's handoff note — but whoever builds `PlacementInputBridge` (PlacementSystem epic) needs to wrap `_grid.world_to_grid(pos, cell_size)`, not call a same-named single-arg method as the ADR's example implies. Consider a short ADR-0001 amendment note when that bridge is built — tracked from `production/epics/grid-system/story-002-grid-solidity-coords.md`
- **2026-08-01** (Story 003: Rotation Transform and Declared Bounds): `declared_bounds(footprint_cells: Array[Vector2i], access_cells: Array[Vector2i])` takes raw typed cell arrays instead of ADR-0003's illustrative `declared_bounds(equipment_def: EquipmentDef)` sketch, because `EquipmentDef` does not exist in `src/` yet — the equipment-catalog epic is entirely out of this sprint's scope. Consequence: the ADR's structural protection ("external callers cannot request a footprint-only bbox because the signature only accepts a whole equipment_def") is weaker than the ADR intended — any caller can currently pass an empty or mismatched `access_cells` and get a footprint-only bbox back. Revisit this signature once `EquipmentDef` exists (equipment-catalog epic) and consider narrowing it back to an `equipment_def`-only signature at that point — tracked from `production/epics/grid-system/story-003-grid-rotation-transform.md`
- ~~**2026-08-01** (Story 003: Rotation Transform and Declared Bounds): `GridSystem.Rotation` enum is degree-valued (`R0=0, R90=90, R180=180, R270=270`), matching GDD D.1's rotation formula directly. ADR-0003's illustrative `PlacedInstance.rotation: int  # 0,1,2,3 (quarter-turn count)` sketch uses a different convention, for a class that does not exist yet (Story 006). Whoever implements Story 005 (commit's rotation param) and Story 006 (`PlacedInstance`) must explicitly decide how `GridSystem.Rotation`'s degree values map to (or replace) that quarter-turn-count convention — do not let the two conventions coexist by accident — tracked from `production/epics/grid-system/story-003-grid-rotation-transform.md`~~ → **2026-08-02 已解决 (Story 006)**: **degrees everywhere.** `PlacedInstance.rotation` stores the degree-valued `GridSystem.Rotation` int (0/90/180/270), exactly matching `PlacementRecord.rotation` (Story 005) and `commit()`'s rotation param. ADR-0003's quarter-turn sketch is superseded; the two conventions will not coexist.
- **2026-08-01** (Story 003: Rotation Transform and Declared Bounds): Resolved Open Question #14 (how to verify `assert()`-triggering acceptance criteria) via **subprocess isolation** — `tests/unit/grid_system/grid_rotation_assert_probe.gd` runs the assert-triggering call in an isolated `godot --headless` child process and checks its combined stdout+stderr for Godot's own "Assertion failed" text. This pattern is reusable for Story 005's similar `push_error()`-must-fire ACs (AC-C7.2/C7.3/C7.7, logged above under the 2026-07-31 Story 002 entry) — worth promoting to a shared test helper if a third story needs it, rather than copy-pasting the probe script again — tracked from `production/epics/grid-system/story-003-grid-rotation-transform.md`
- **2026-08-01, CORRECTED same day** (Story 003): the entry directly above originally stated "`assert()` halts only the current call stack, not the process." **That was incomplete and led to a real bug that shipped past two rounds of self-review before `/code-review` caught it.** The corrected, empirically-verified finding: `assert(false, msg)` in Godot 4.7.1 aborts the rest of the **current function frame** when it fires — no statement textually after it in that function executes, including a `return`. A **value-typed** return (`Vector2i`, `int`, `bool`, ...) silently yields that type's zero-default when the frame aborts (harmless-looking, easy to miss); an **Object-typed** return (any `RefCounted`/`Node`-based class, including this story's own `TransformedFootprint`) yields `null`, which crashes the caller the moment it dereferences the result. This is why `get_transformed_cells()`'s illegal-rotation guard uses `push_error()` only — never `assert()` — while returning its `TransformedFootprint` result: `assert()` followed by `return result` in the same frame would make the `return` unreachable dead code. `_transform_cell()`'s own `assert(false)` fallback (its return type is `Vector2i`, a value type) is safe by the same logic and is kept as defense-in-depth, though it is unreachable via the only current public call path. **Action item**: audit `declared_bounds()`'s two debug asserts — both precede a `Vector2i` return, so they're safe under this corrected understanding, but this should be a conscious re-check (not an assumption) for any future story that adds an `assert()` ahead of an Object-typed return — grep the codebase for `assert(` followed by a `return` of a `RefCounted`/`Node`-typed value before trusting any new one — tracked from `production/epics/grid-system/story-003-grid-rotation-transform.md`
- **2026-08-01** (Story 003: Rotation Transform and Declared Bounds): AC-D1.1's literal text says "WHEN in debug build calling `get_transformed_cells`, THEN the default branch `assert(false, ...)` triggers." As a direct consequence of the fix in the entry above, that assert branch (inside `_transform_cell()`) is now structurally unreachable via the only public call path — `get_transformed_cells()` rejects illegal rotations via `push_error()` before `_transform_cell()` is ever invoked. The AC's underlying intent ("never a silent fallthrough to 0 degrees") is satisfied more strongly than the letter asks (illegal input cannot reach the fallback at all, rather than reaching it and continuing past it), but the assert itself is no longer observable through `get_transformed_cells()`. If a future story needs to literally exercise `_transform_cell()`'s assert branch (e.g. for a stricter interpretation of TR-GS-029), it would require calling the private method directly, which this project's testability convention deliberately avoids — tracked from `production/epics/grid-system/story-003-grid-rotation-transform.md`
- **2026-08-02** (Story 004: Placement Validation — can_place): `can_place(footprint_cells, access_cells, anchor, rotation)` takes raw typed cell arrays instead of ADR-0003's illustrative `can_place(def: EquipmentDef, ...)` sketch, for the same reason as the Story 003 `declared_bounds()` deviation directly above — `EquipmentDef` does not exist in `src/` yet (equipment-catalog epic out of scope). Same consequence class: the ADR's structural protection (signature can only accept a whole equipment_def, so footprint/access always come together) is weaker — a caller can pass mismatched/empty access arrays. Additionally, ADR-0003's sketch types `rotation` as a bare int (its `PlacedInstance.rotation` uses quarter-turn count 0,1,2,3); can_place takes the degree-valued `GridSystem.Rotation` enum for consistency with `get_transformed_cells()`. Both deviations collapse into the equipment-catalog / PlacedInstance reconciliation already tracked from Story 003 — revisit when those types exist — tracked from `production/epics/grid-system/story-004-grid-can-place.md`
- ~~**2026-08-02** (Story 005: Commit, Clear, and Reverse Index): **Rotation convention decided, closing Story 005's half of the Story 003 handoff.** `commit()` takes the degree-valued `GridSystem.Rotation` enum (0/90/180/270) and `PlacementRecord.rotation` stores that degree int — NOT the quarter-turn count (0,1,2,3) in ADR-0003's `PlacedInstance.rotation` sketch. `PlacedInstance` still does not exist (Story 006); whoever builds it must reconcile or replace the quarter-turn convention explicitly. The two conventions must not coexist by accident — this entry keeps the reconciliation visible until then — tracked from `production/epics/grid-system/story-005-grid-commit-clear.md`~~ → **2026-08-02 已解决 (Story 006)**: `PlacedInstance.rotation` stores the same degree int as `PlacementRecord.rotation` — degrees everywhere, reconciliation closed.
- **2026-08-02** (Story 005: Commit, Clear, and Reverse Index): **This is the third story needing "must push_error()" verification** (after Story 002's AC-D2.2 and Story 003's assert probe), and the second to build a subprocess-isolation probe — `tests/unit/grid_system/grid_commit_clear_error_probe.gd` copies `grid_rotation_assert_probe.gd`'s structure (run the rejection path in an isolated child `godot --headless`, assert on merged output containing "ERROR:" plus a clean exit 0). The Story 003 entry already suggested promoting this to a shared test helper "if a third story needs it" — this is the third; **the promotion is now warranted** (e.g. a `tests/support/probe_runner.gd` + per-story probe scripts, or a single parameterized error-probe). Also learned this round: probe scripts must pass TYPED arrays through `Object.call()` — Godot's typed-array parameter boundary rejects untyped literals with a SCRIPT ERROR that pollutes the very output the probe measures (the first probe version's error modes "passed" spuriously for exactly this reason; the clean-control mode caught it) — tracked from `production/epics/grid-system/story-005-grid-commit-clear.md`
- **2026-08-02** (Story 005): Two defensive strengthenings over the Story 005 implementation sketch, both protecting against the GDD's silent-corruption class rather than deviating from its intent: (1) `PlacementRecord._init()` duplicates its cell arrays — the record OWNS its data, so a caller that mutates/reuses scratch arrays after `commit()` cannot corrupt the reverse index (the sketch stored caller arrays by reference); (2) `commit()`/`clear()` emit duplicated payload arrays — a signal subscriber mutating the payload cannot corrupt the stored record (consistent with `get_access_ids()`'s existing duplicate-before-return posture). Additionally `commit()` dedups access ids per cell, without which a duplicate access cell in the input would leave a permanently leaked access registration after `clear()`'s single-occurrence `Array.erase()`. Low risk, no known downside — recorded so reviewers can judge the deviations deliberately rather than assume the sketch was followed literally — tracked from `production/epics/grid-system/story-005-grid-commit-clear.md`
- **2026-08-02** (Story 006: GridStateReader and GridSnapshot): **deviation from Story 006 sketch — `GridStateReader extends SimSystem` (ADR-0003's governing hierarchy), NOT `RefCounted` directly as the story's illustrative sketch shows.** Story sketch is simplified; ADR-0003's hierarchy diagram (`RefCounted → SimSystem → GridStateReader → GridSystem/GridSnapshot`) is the governing contract, and GridSystem must keep SimSystem's two-phase init machinery through a single inheritance chain. Documented in the class header — tracked from `production/epics/grid-system/story-006-grid-state-reader-snapshot.md`
- **2026-08-02** (Story 006): **subprocess probe pattern now used a FOURTH time** — `grid_state_reader_error_probe.gd` (abstract-guard + stub push_error verification). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/grid-system/story-006-grid-state-reader-snapshot.md`
- **2026-08-02** (Story 006): `GridSystem.get_placed_instances()` populates `PlacedInstance.equipment_id` as `""` — GridSystem deliberately stores only integer occupant_id (never equipment type, per its header), so equipment identity is unknown at this layer. The equipment-catalog epic will resolve instance_id → equipment_id and (optionally) extend commit() to carry it. Until then, consumers needing equipment identity must go through PlacementSystem/catalog — tracked from `production/epics/grid-system/story-006-grid-state-reader-snapshot.md`
- **2026-08-02** (Story 006): `PlacedInstance.anchor` is DERIVED as the min-offset of footprint ∪ access rather than stored — correct for can_place-validated placements by the AC-D5.2 anchor convention (canonical min offset is (0,0), so the transformed min equals the anchor), and the reverse index currently has no anchor field. If a future caller commits non-normalized cells, the derivation silently degrades — revisit if commit() ever accepts raw (non-can_place) input — tracked from `production/epics/grid-system/story-006-grid-state-reader-snapshot.md`
- **2026-08-02** (Story 006): `Script.has_method()` does NOT reflect GDScript-declared methods in 4.7.1 (verified empirically while writing AC-GSR.3's static check) — the test walks `get_script_method_list()` instead. Worth noting for any future reflection-based tooling — tracked from `production/epics/grid-system/story-006-grid-state-reader-snapshot.md`
- **2026-08-02** (Story 007: Serialization): **`Vector2i` cannot survive a JSON round-trip in 4.7.1** (verified empirically — probe output showed `JSON.stringify` rendering Vector2i as the string `"(1, 2)"`, and `JSON.parse_string` returning that string back). Consequence: `serialize()` emits cells as `[x, y]` int arrays (matching ADR-0002's catalog format) instead of the Story 007 sketch's raw `rec.footprint_cells`; `deserialize()` accepts BOTH encodings (Vector2i for in-memory round-trips, `[x,y]` arrays from JSON files). **The SaveLoad epic MUST NOT assume `GridSystem.serialize()` output can be `JSON.stringify`'d directly — it already can** (cells are JSON-safe), but any future system that serializes Vector2i directly into a save blob will hit this. Also probed: `JSON.parse_string` returns FLOATS for all numbers — deserialize coerces via `int()` — tracked from `production/epics/grid-system/story-007-grid-serialization.md`
- **2026-08-02** (Story 007): `deserialize()` signature reconciled as `deserialize(data, buildable_snapshot, mode)` — the story sketch's 3-arg form, superseding ADR-0002's `(data, mode)` Key-Interfaces sketch and GDD §C.8's `(data, buildable_snapshot)` form. buildable is injected separately (TR-GS-020); mode is "validate" (Phase A only) or "commit" (Phase A + Phase B). Return type is the new `DeserializeResult` DTO (not ADR-0002's `Dictionary` sketch) — SaveLoad consumes `DeserializeResult.errors[{category,message,detail}]` — tracked from `production/epics/grid-system/story-007-grid-serialization.md`
- **2026-08-02** (Story 007): error taxonomy extends GDD's three display categories with `CORRUPTED_SAVE` (structural corruption: schema_version mismatch, negative/duplicate instance_id, illegal rotation, empty footprint, malformed cells) and `INTERNAL_ERROR` (unknown mode, short buildable_snapshot, use-before-init). The GDD's "仅...三种" line enumerates the SaveLoad/UI display categories; structural and programming failures are distinct classes that should not be mis-labeled as geometry problems — tracked from `production/epics/grid-system/story-007-grid-serialization.md`
- **2026-08-02** (Story 007): signal-emission deviation from the sketch's unconditional emit — a completely empty load into an already-empty grid emits NOTHING (nothing changed, per the story's own QA edge-case note); emptying a populated grid DOES emit (the clear is a real change). Failed loads never emit. All other success paths emit exactly once with the union payload (AC-C8.10) — tracked from `production/epics/grid-system/story-007-grid-serialization.md`
- **2026-08-02** (Story 008: Signals, Integration, and Performance): **AStarGrid2D 4.7.1 API deviation from ADR-0005's illustrative sketch (verified empirically by probe).** ADR-0005 §1 and §3's `_on_grid_changed` example shows `_astar.set_point_solid(cell.x, cell.y, solid)` — three int args. The real 4.7.1 signature is `set_point_solid(position: Vector2i, solid: bool)`. Additionally, the grid MUST be initialized with `update()` BEFORE any `set_point_solid()` call (else \"Grid is not initialized\" error, probed); once initialized, `set_point_solid()` takes effect immediately with no second `update()` needed (GDD C.4 engine note confirmed — identical paths with/without trailing update()). **The Navigation epic (system #5) MUST read `tests/integration/grid_system/grid_navigation_solidity_test.gd`'s `_bake_astar_from_grid()` before writing its grid_changed subscription handler** — copy the correct call order there, not the ADR sketch — tracked from `production/epics/grid-system/story-008-grid-signals-integration-perf.md`
- **2026-08-02** (Story 008): `get_solidity_snapshot()` added to GridSystem (the GDD's Navigation read surface, lines 315/739, required by AC-X.1) — the read-surface contract in ADR-0003 §1 was written before the GDD's interaction table listed it, so the batch solidity query lives ONLY on GridSystem (not on the GridStateReader abstract contract). Navigation holds a GridSystem reference per the GDD dependency table, so this is sufficient; if a future consumer needs a solidity batch query over a speculative GridSnapshot, that would be a separate GridStateReader contract extension (ADR amendment) — tracked from `production/epics/grid-system/story-008-grid-signals-integration-perf.md`
- **2026-08-02** (Story 008): subprocess probe pattern now used a FIFTH time — `grid_guardrail_error_probe.gd` (AC-NEG.2's commit/clear-on-unreachable-equipment zero-ERROR verification). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/grid-system/story-008-grid-signals-integration-perf.md`
- **2026-08-02** (Story 001: EquipmentDef Data Model and Catalog Container): **three deliberate strengthenings over the Story 001 sketch, all in service of the BLOCKING ACs**: (1) `get_definition()` returns a DEEP COPY of the stored record (`EquipmentDef.duplicate_def()`) instead of the sketch's shared-instance return — the sketch as-written fails BLOCKING AC-IMMUTABLE.1's literal requirement (caller appending to the returned instance's `footprint_cells` would mutate the stored definition), and copy-on-return is the resolution the AC's own "(deep copy or immutable-by-convention)" clause sanctions; (2) `has_definition()` carries the before-freeze `push_error()` guard the sketch omitted — per the QA edge case ("verify has_definition() errors") and the Control Manifest guardrail ("is_frozen check on every public method"); (3) `_freeze()` fires `assert()` on a second call — per the QA edge case ("verify _freeze() cannot be called twice"). All verified by the new subprocess probe `tests/unit/equipment_catalog/equipment_catalog_error_probe.gd` — tracked from `production/epics/equipment-catalog/story-001-equipment-def-catalog.md`
- **2026-08-02** (Story 001): **typed-array boundary pitfall, verified empirically in 4.7.1**: `var ids: Array[String] = _definitions.keys()` raises `SCRIPT ERROR: Trying to assign an array of type "Array" to a variable of type "Array[String]"` — untyped `Dictionary.keys()` does NOT implicitly convert in a typed var initializer. Fix: `var ids: Array[String] = []` + `ids.assign(_definitions.keys())`. Extends the Story 005 typed-array lesson (untyped literals rejected at `Object.call()` boundaries): 4.7.1 is strict about untyped→typed Array conversion in both directions — tracked from `production/epics/equipment-catalog/story-001-equipment-def-catalog.md`
- **2026-08-02** (Story 001): subprocess probe pattern now used a SIXTH time — `equipment_catalog_error_probe.gd` (AC-FROZEN.1/AC-FROZEN.2 push_error + assert verification). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/equipment-catalog/story-001-equipment-def-catalog.md`
- **2026-08-02** (Story 002: JSON Loading and Anchor Normalization): **Godot 4.7.1 `JSON.parse()` parses ALL JSON numbers as float — even integer literals** (empirically verified: `"200"` → `200.0`, `"-3"` → `-3.0`, `"0"` → `0.0`; `type_string(typeof(v))` returns "float", `v is int` is false). This extends the Story 007 observation (`JSON.parse_string` returns floats) to the instance-based `JSON.new().parse()` path the catalog loader uses. Consequence: any loader parsing integer fields from JSON MUST accept integer-valued floats (`v is float and is_equal_approx(v, round(v))` → `int(v)`) or every numeric field in a hand-authored file is rejected. Implemented as `EquipmentCatalogLoader._is_int_value()`. The engine-reference docs do not document this — consider adding a note to `docs/engine-reference/godot/` — tracked from `production/epics/equipment-catalog/story-002-json-loading-anchor-normalization.md`
- **2026-08-02** (Story 002): **Godot 4.7.1's JSON parser LENIENTLY accepts trailing commas** (empirically verified: a file ending `],\n}` parses with `err=0, line=0`). The QA test case "file with trailing comma" therefore CANNOT produce a parse error — AC-JSON.2 is exercised with a missing-comma syntax error fixture instead (`syntax_missing_comma.catalog.json`); both malformed fixtures report the error at line 3 (the parser's confusion point, verified empirically). If strict JSON validation ever becomes a requirement (e.g. linting designer-authored files in CI), a stricter parser or a schema validator would be needed — tracked from `production/epics/equipment-catalog/story-002-json-loading-anchor-normalization.md`
- **2026-08-02** (Story 002): **LoadError/LoadResult shapes follow the Story 002/004 DTO sketches, diverging from ADR-0002 section 7.** ADR-0002 sketches `LoadError {category, message, detail}` and `CatalogLoadResult {definitions, errors, ok}`; the story sketches (later, more specific, and what downstream Story 004 explicitly consumes) define `LoadError {equipment_id, category, message}` (`detail` folded into `message`; per-entry errors carry the offending entry id) and `LoadResult {ok, catalog, errors}` with static `fail(category, message)` + `new_loaded(catalog, errors)` factories (ok = frozen catalog has ≥1 definition). Named `LoadResult`, not `CatalogLoadResult`. Story 004's DTO block was implemented verbatim — recorded so a reviewer can judge the ADR deviation deliberately — tracked from `production/epics/equipment-catalog/story-002-json-loading-anchor-normalization.md`
- **2026-08-02** (Story 002): **ADR-0002's `format_version` version envelope is NOT enforced by the catalog loader** — Story 002's sketch checks `data.has("equipment")` at the JSON root (no `format_version` / `payload` wrapper), which is what the loader implements; a file like `{"format_version": 1}` without `equipment` fails with INVALID_SCHEMA. The ADR's envelope contract remains unimplemented for the catalog category — the validation-pipeline story (004+) should decide whether to add version validation before the storage epic closes — tracked from `production/epics/equipment-catalog/story-002-json-loading-anchor-normalization.md`
- **2026-08-02** (Story 002): **`zone_membership` accepts only Array-of-String** (Story 002's schema), NOT ADR-0002 section 4's single-string form (`"zone_membership": "cardio"`). The EquipmentDef field is typed `Array`; a single string is rejected with an INVALID_ENTRY error naming the field. If the ADR's single-string form is ever wanted as a convenience, the loader needs a leniency decision — tracked from `production/epics/equipment-catalog/story-002-json-loading-anchor-normalization.md`
- **2026-08-02** (Story 002): **subprocess probe pattern now used a SEVENTH time** — `equipment_catalog_loader_error_probe.gd` (strict_mode=true assert verification). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/equipment-catalog/story-002-json-loading-anchor-normalization.md`
- **2026-08-02** (Story 003: Footprint Shape and Access Cell Validation): **GDScript forbids a member variable and a method sharing the same name** (empirically verified: `var ok: bool` + `static func ok()` in the same class → `Parse Error: Function "ok" has the same name as a previously declared variable`). The Story 003 sketch's `ValidationResult.ok()` factory cannot coexist with the `.ok` property the sketch's own pipeline reads (`if not r.ok`). Resolution: property `ok` wins (it's the pipeline idiom), passing factory renamed to `success()`. Anyone porting the sketch literally will hit this — tracked from `production/epics/equipment-catalog/story-003-footprint-access-validation.md`
- **2026-08-02** (Story 003): **`abs()` returns Variant in 4.7.1** (empirically verified: `var dx := abs(ac.x - fc.x)` trips the project's "inferred from Variant" warning treated as error). The global `abs()` helper is not statically typed; use `var dx: int = abs(...)` for int operands. Same class of issue as the Story 001 typed-array lesson — GDScript's strict-typing warning set catches untyped builtin returns — tracked from `production/epics/equipment-catalog/story-003-footprint-access-validation.md`
- **2026-08-02** (Story 003): **subprocess probe pattern now used an EIGHTH time** — `equipment_shape_validation_error_probe.gd` (AC-C.1 strict assert + AC-C.2 non-strict push_error verification). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/equipment-catalog/story-003-footprint-access-validation.md`
- **2026-08-02** (Story 004: Validation Pipeline, strict_mode, and Duplicate ID Detection): **AC-PIPELINE.3 is internally contradictory in the story, resolved toward the detailed spec (3:1).** The BLOCKING AC text says "only the FIRST failure is reported (deterministic error ordering)"; the story's own QA Test Cases section says "returns 3 errors (FOOTPRINT_EMPTY, ACCESS_COUNT, COST_NEGATIVE) — NOT just the first" and the Implementation Notes' Key design decisions say "_validate_all collects ALL errors per entry, not first-only (unlike individual validators which early-exit)". Resolved toward collect-all-per-sub-validator: `_validate_all` runs every sub-validator in fixed order and collects one error per FAILING sub-validator (each sub-validator still early-exits at its own first failure). This reading also makes the AC's "(deterministic error ordering)" parenthetical meaningful (ordering is meaningless with a single error). The kanban card's Chinese summary ("多失败仅报第一条") follows the AC text; the reviewer should note this resolution when checking AC-PIPELINE.3 coverage — tracked from `production/epics/equipment-catalog/story-004-validation-pipeline-strict-mode.md`
- **2026-08-02** (Story 004): **collect-all pipeline changes the EC-003 loader-path observable** — wiring `_validate_all` into `_load_single_definition` means `empty_footprint.catalog.json`'s bad entry now reports TWO errors (FOOTPRINT_EMPTY from the footprint validator AND ACCESS_NOT_ADJACENT from the access validator — with zero footprint cells there is nothing to be adjacent to) instead of one. The EC-003 test assertion `errors.size() == 1` was updated to `== 2` with both rule codes checked (67→69 assertions). This is the intended Story 004 semantic change, not a regression; AC-C.1/C.2 (entry excluded, push_error, valid entries load) are unaffected — tracked from `production/epics/equipment-catalog/story-004-validation-pipeline-strict-mode.md`
- **2026-08-02** (Story 004): **duplicate-id check runs BEFORE structural parsing** (Step 0 in the story sketch) via a new `_raw_entry_id()` helper that extracts the raw String id without parsing the entry. Two deliberate micro-decisions: (1) empty ids are NOT tracked in `seen_ids` — a missing id is a structural INVALID_ENTRY failure (Story 002 parser), not a duplicate, so two id-less entries do not falsely collide; (2) non-strict duplicate exclusion fires push_error() — the GDD Edge Cases say duplicates "走上一条的失败处理路径" (take the same failure path as validation failures), which per AC-C.2/EC-003 decision includes push_error — tracked from `production/epics/equipment-catalog/story-004-validation-pipeline-strict-mode.md`
- **2026-08-02** (Story 004): **subprocess probe pattern now used a NINTH time** — `catalog_pipeline_strict_mode_probe.gd` (AC-E.1 strict duplicate assert, AC-PIPELINE.2 strict invalid assert, non-strict duplicate push_error + KEPT_COST proof). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/equipment-catalog/story-004-validation-pipeline-strict-mode.md`
- **2026-08-02** (Story 005: Use-Duration Field Validation): **AC-U.3 QA sub-case (d) `min=300 > max=200` reports USE_DURATION_MIN_EXCEEDS_MEAN, not USE_DURATION_RANGE_INVALID — the RANGE_INVALID branch is mathematically unreachable.** The ordered checks (faithful to the story's implementation sketch) test `min > mean` (rule g) before `min > max` (rule h 2nd clause). With mean=200, min=300 trips (g) first. More strongly: because (g) requires `min <= mean` and (h) 1st clause requires `max >= mean`, any input reaching the (h) 2nd clause already satisfies `min <= mean <= max` — so `min > max` can never be true there. The branch is defensive dead code; the AC only requires the entry to FAIL for `min > max`, which it does deterministically via (g) or (h) 1st clause. Kept in the implementation for sketch fidelity, documented in the test, and the (d) sub-case asserts the actual observed code — tracked from `production/epics/equipment-catalog/story-005-use-duration-validation.md`
- **2026-08-02** (Story 005): **`_validate_all` signature extended to carry the 4 parsed use-duration ints** — the Story 004 implementation's `_validate_all(footprint_cells, access_cells)` becomes `_validate_all(footprint_cells, access_cells, use_mean, use_stddev, use_min, use_max)`. The two direct `_validate_all(...)` calls in the EC-004 pipeline test were updated to pass valid values (200/35/100/300) so that test still isolates footprint/access ordering (2 failures, unchanged assertions). `validate_use_duration` itself deviates from the sketch's `entry: Dictionary` signature to typed ints — the loader already parses + structurally validates the 4 fields (`_field_int` rejects missing/non-integer, enforcing the Control Manifest "missing = validation failure" upstream), matching the Story 003 typed-validator convention — tracked from `production/epics/equipment-catalog/story-005-use-duration-validation.md`
- **2026-08-02** (Story 005): **subprocess probe pattern now used a TENTH time** — `catalog_use_duration_strict_probe.gd` (AC-U.1 strict mean-zero assert + non-strict mean-zero exclusion push_error proof). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/equipment-catalog/story-005-use-duration-validation.md`
- **2026-08-02** (Story 006: Provisional Cost Formula): **the formula lives in a dedicated static class `EquipmentCostFormula` (`src/systems/equipment_cost_formula.gd`), NOT on `EquipmentCatalog` or `EquipmentDef`** — Story 001's AC-C.8 test statically asserts the catalog's public API is exactly the 3 read-only queries via `get_script_method_list()`, and any public method (static included) would break it; `EquipmentDef` is a logic-free DTO. **Interface contract for Story 002 (JSON loader, running in parallel)**: call `EquipmentCostFormula.compute_provisional_cost(normalized_footprint_cells)` at EquipmentDef construction time when a JSON entry omits `"cost"`; use the explicit JSON value when present. `validate_cost()` (cost<0 rejected, cost=0 allowed) is deliberately NOT implemented here — it belongs to Story 007's AC-E.2 and references Story 004's `ValidationResult` type, which does not exist yet — tracked from `production/epics/equipment-catalog/story-006-provisional-cost-formula.md`
- **2026-08-02** (Story 007: Edge Cases — Empty Catalog, Unlock Requirements, Cost Boundary): **the story sketch's `get_definition_count()` is NOT implemented — it conflicts with Story 001's AC-C.8, which pins the catalog's public API to EXACTLY 3 read-only queries** (`get_all_ids` / `get_definition` / `has_definition`) via `get_script_method_list()`; adding a 4th public method would break that BLOCKING AC's static test (same constraint that forced `EquipmentCostFormula` off the catalog in EC-006). The BLOCKING AC-E.6 text only requires `get_all_ids()` → `[]` and `get_definition(any)` → `push_error()+null`, both of which are covered with the existing 3 queries; `has_definition(any)` → false covers the QA's `get_definition_count()` intent (count queryable as `get_all_ids().size()`) — tracked from `production/epics/equipment-catalog/story-007-edge-cases-empty-unlock-cost.md`
- **2026-08-02** (Story 007): **`cost` became an OPTIONAL JSON field, resolving the EC-006 interface contract** — previously (Story 002) a missing `"cost"` key was a structural INVALID_ENTRY error; now (Story 007, AC-E.2 QA edge "cost explicitly omitted from JSON (should use formula)") a missing cost is legal and `EquipmentCatalogLoader._load_single_definition` computes `EquipmentCostFormula.compute_provisional_cost(normalized footprint)` at construction. Explicit cost still wins; a present-but-wrong-typed cost is still a structural error. The formula output is always ≥ 0 so `validate_cost()` can never reject a formula-derived value — COST_NEGATIVE fires only for explicit negative costs — tracked from `production/epics/equipment-catalog/story-007-edge-cases-empty-unlock-cost.md`
- **2026-08-02** (Story 007): **`unlock_requirement` became an OPTIONAL JSON field** — previously (Story 002) a missing key was a structural INVALID_ENTRY error; now (Story 007, AC-E.3 QA edge "null/missing field (should default to '')") missing and null both map to `""` (empty string = always available, GDD Core Rule 1). Present-but-non-string is still a structural error. This relaxes Story 002's required-fields contract for exactly this one field, per the story's Implementation Notes sketch (`var unlock_req: String = entry.get("unlock_requirement", "")`) — tracked from `production/epics/equipment-catalog/story-007-edge-cases-empty-unlock-cost.md`
- **2026-08-02** (Story 007): **wiring `validate_cost()` into `_validate_all` changes the EC-004 loader-path observable as EC-004 explicitly deferred** — `pipeline_multi_failure.catalog.json`'s `multi_failure_rack` (empty footprint + 2 access + cost=-1) now reports THREE errors in the documented fixed order FOOTPRINT_EMPTY → ACCESS_COUNT → COST_NEGATIVE (was 2). The EC-004 parent handoff recorded "pipeline_multi_failure fixture 的负数 cost 暂不产生 COST_NEGATIVE（Story 007 加入）"; the pipeline test was updated 52→55 assertions to match the story QA case's exact 3-error order. This is the intended cross-story semantic change, not a regression — tracked from `production/epics/equipment-catalog/story-007-edge-cases-empty-unlock-cost.md`
- **2026-08-02** (Story 007): **subprocess probe pattern now used a TENTH time** — `catalog_edge_cases_probe.gd` (AC-E.5 strict assert on first invalid, non-strict push_error count = 3, AC-E.6 empty-catalog push_error + null, AC-E.3 no-error/no-warning). The promotion to a shared helper (e.g. `tests/support/probe_runner.gd`) remains open — tracked from `production/epics/equipment-catalog/story-007-edge-cases-empty-unlock-cost.md`

---

## Pre-existing issues surfaced during Story 001 review

Not caused by Story 001; verified against a clean baseline. Logged here so they
are not rediscovered from scratch.

- ~~**2026-07-25** (found during Story 001 review): `tests/integration/core_loop/core_loop_test.gd` fails to load — every `preload()` uses a `res://../../prototypes/...` path that does not resolve. The test never runs, so `tests/headless_runner.gd` reports success while silently skipping it.~~ → **2026-07-30 部分解决**：不再冒充通过。文件已登记进 runner 的 `PENDING_FILES`，每轮显式打印为 `SKIPPED` 并附解锁条件。preload 路径本身仍未修 —— 需等 core 层 epic（PlacementSystem / Navigation / MemberSim / Congestion）的 `src/` 实现产出后按真实 API 重写，见文件头注释。
- ~~**2026-07-25** (found during Story 001 review): `tests/headless_runner.gd`'s `_run_file()` calls `instance.run_all()`, but `run_all()` re-invokes `_init()` — which the engine already ran on `script.new()`. Every test file executes twice through the CI entry point, doubling reported assertion counts.~~ → **2026-07-30 已解决**：测试文件契约改为 `run_all() -> Dictionary`，`_init()` 检测 `RUNNER_META` 后立即返回。实测断言数由 130 回落到真实的 65。
- ~~**2026-07-25** (found during Story 001 review): `tests/smoke/core_smoke_test.gd` preloads implementation files from `prototypes/gym-flow-vertical-slice/`, violating `.claude/rules/prototype-code.md`.~~ → **2026-07-30 部分解决**：已登记进 `PENDING_FILES`，不再运行、不再计入通过。仍需按 `src/` API 重写 —— 解锁条件为 grid-system story-002/004/005/006 + time-system story-003。

### 2026-07-30 修 runner 时新发现并当场解决的问题

- **2026-07-30**（严重）：`tests/headless_runner.gd` 从不汇总各文件的 pass/fail —— `_total_fail` 只在 load 失败时自增，`instance.run_all()` 的返回值被赋给未使用的 `ok` 变量，`_print_summary()` 不打印任何数字。**结果是任何测试失败仍以退出码 0 结束**，`.github/workflows/tests.yml` 声称的"测试失败阻止合并"门禁完全无效。已用一个故意失败的探针实测确认（EXIT = 0），修复后同样探针得到 EXIT = 1。
- **2026-07-30**：`.github/workflows/tests.yml` 本身无法运行 —— 同一 step 里同时写了 `uses: ./`（仓库根目录并无 action.yml）和 `run:`，属非法 Actions 语法，且完全没有安装 Godot 的步骤。已重写为下载官方 `Godot_v4.7.1-stable_linux.x86_64.zip`（带 actions/cache）后执行 runner。**尚未在真实 CI 上跑过 —— 需一次 push 或 PR 才能验证。**
- **2026-07-30**：解析失败的脚本 `load()` 仍返回非 null 的 GDScript 对象，随后的 `script.new()` 以运行时脚本错误中止 `_run_file()`，导致该文件从报告中彻底消失（探针实测：`probe_broken.gd` 既不在汇总里也不影响退出码）。已加 `script.can_instantiate()` 前置检查。
- **2026-07-30**：孤儿测试文件无人发现 —— 写了测试但忘记加进 `TEST_FILES` 时，CI 会一片绿。已加 `_check_registry_coverage()`：扫描 `tests/**/*_test.gd`，任何未在 `TEST_FILES` / `PENDING_FILES` 登记的文件记为失败。同时把"`run_all()` 报告 0 个断言"也判为失败。
- **2026-07-30**（顺带）：测试脚本多为 `extends SceneTree`（非 RefCounted），runner 从不释放实例，每轮留下 32 个 ObjectDB 泄漏 + Canvas/Viewport RID 泄漏警告。已加 `_release()`，警告消失。
