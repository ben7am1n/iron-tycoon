# QA Plan: Sprint 2 — Foundation 收官（time-system + save-load）

**Date**: 2026-08-02
**Generated by**: /qa-plan
**Scope**: 8 stories across 2 systems
**Engine**: Godot 4.7.1 (GDScript)
**Sprint File**: `production/sprints/sprint-2.md`

---

## Test Summary

| Story | Type | Automated Test Required | Manual Verification Required |
|-------|------|------------------------|------------------------------|
| TS-001 SimulationOrchestrator and Tick Dispatch | Logic | Unit test — `tests/unit/time_system/orchestrator_tick_dispatch_test.gd` | None |
| TS-002 Tick Accumulator, Speed Control, and Pause | Logic | Unit test — `tests/unit/time_system/tick_accumulator_test.gd` | None |
| TS-003 SeededRNG and Sub-Stream Derivation | Logic | Unit test — `tests/unit/time_system/seeded_rng_substream_test.gd` + `lsr_helper_test.gd` | None |
| TS-004 Serialization, Deserialization, and Resume | Logic | Unit test — `tests/unit/time_system/time_serialization_test.gd` | None |
| SL-001 SaveBlob Composition and Tick-Boundary Hook | Integration | Integration test — `tests/integration/save_load/saveblob_composition_test.gd` | None |
| SL-002 Load Orchestration — Phase A/B and Load Order | Integration | Integration test — `tests/integration/save_load/load_orchestration_test.gd` | Smoke |
| SL-003 Round-Trip Determinism and Resume-Paused | Integration | Integration test — `tests/integration/save_load/roundtrip_determinism_test.gd` | Smoke |
| SL-004 File I/O, JSON Encoding, and Version Checking | Integration | Integration test — `tests/integration/save_load/file_io_version_test.gd` | None |

---

## Automated Tests Required

### TS-001 — SimulationOrchestrator and Tick Dispatch (Logic)
**Test file path**: `tests/unit/time_system/orchestrator_tick_dispatch_test.gd`
**What to test**:
- AC-INIT-1: init() 二次调用 → assert() 触发
- AC-INIT-2: init() 前调用任意公开方法 → push_error() + 安全默认值
- 拓扑初始化顺序（Tier 0-7）由 _ready() 强制
- tick 固定序列（MemberSim → Congestion → Satisfaction → Economy 直接调用，非信号驱动）
- tick_completed(tick_count) 信号 arity 与时机（每 tick 序列末尾恰一次）
- AC-NO-AWAIT: 静态 grep 确认所有 on_tick() 无 await/yield

**Edge cases**: 空 tick 序列、tick_completed 订阅者数量变化

**Estimated test count**: ~35 assertions

### TS-002 — Tick Accumulator, Speed Control, and Pause (Logic)
**Test file path**: `tests/unit/time_system/tick_accumulator_test.gd`
**What to test**:
- TICK_DURATION_SECONDS = 0.1 固定步进（accumulator 累积→扣减循环）
- MAX_TICKS_PER_FRAME = 8 catch-up 上限
- speed 1x/2x/3x 切换不重置 accumulator、不触碰 tick_count
- pause（speed_multiplier=0）冻结全部状态；pause/resume 不推进 tick_count
- 同 seed + 同 tick 序列 → replay 位一致（确定性与帧率无关）

**Edge cases**: delta=0、超大 delta（单帧 catch-up 上限）、speed 切换恰在 tick 边界

**Estimated test count**: ~30 assertions

### TS-003 — SeededRNG and Sub-Stream Derivation (Logic)
**Test file path**: `tests/unit/time_system/seeded_rng_substream_test.gd` + `tests/unit/time_system/lsr_helper_test.gd`
**What to test**:
- AC-LSR-1: lsr(0x8000000000000000, 30) == 0x0000000200000000（逻辑右移，非符号扩展）
- FNV-1a64 → XOR → SplitMix64 子流派生公式（钉死常量）
- register_system(name) 恰一次；重复注册 = 硬错误
- get_rng(name) 幂等：返回同一实例（携带已推进状态），绝不创建/重播种
- 不同 system 子流独立（不因调用顺序互相影响）

**Edge cases**: 高位 set 的种子值、空名注册、子流种子碰撞验证

**Estimated test count**: ~40 assertions + 子进程 probe

### TS-004 — Serialization, Deserialization, and Resume (Logic)
**Test file path**: `tests/unit/time_system/time_serialization_test.gd`
**What to test**:
- serialize() 输出 `{tick_count, master_seed, per_system_rng_states: {name: state}, speed_multiplier, paused}` 完整键
- deserialize() 恢复 RNG 内部状态（`rng.state = hex_to_int()` 直接恢复，非重派生）
- tick_count/speed_multiplier/paused 恢复
- Core Rule 9: 加载后始终 paused（无论存档 speed）
- 状态恢复后继续 tick 与未保存状态一致

**Edge cases**: RNG 状态已推进多步、paused=true 存档、speed=2x 存档

**Estimated test count**: ~35 assertions

### SL-001 — SaveBlob Composition and Tick-Boundary Hook (Integration)
**Test file path**: `tests/integration/save_load/saveblob_composition_test.gd`
**What to test**:
- AC-BLOB-1: 返回 blob 恰有 8 键 `{version, master_seed, time_system, grid_system, member_sim, congestion, satisfaction, economy}` — 无多无缺
- AC-BLOB-2: 顶层 master_seed 与 time_system.master_seed 一致（冗余非分歧）
- AC-BLOB-3: navigation/placement_system/selection_system/zone_rules 4 系统缺席
- 保存仅在 tick 边界（TimeSystem no-mid-tick-yield 结构性保证）

**Edge cases**: 空状态保存、6 系统 serialize 部分失败

**Estimated test count**: ~25 assertions

### SL-002 — Load Orchestration — Phase A/B and Load Order (Integration)
**Test file path**: `tests/integration/save_load/load_orchestration_test.gd`
**What to test**:
- Phase A: 所有系统 validate-only 模式零变更运行
- Phase B: 全部通过后按序提交
- 加载顺序程序化强制（TimeSystem → GridSystem → Placement.rederive → Selection.rebuild → Navigation.rebuild → MemberSim → Congestion → Satisfaction → Economy）
- 任一失败 → 整体中止，当前会话零变更（all-or-nothing）
- Phase B 失败 → fatal-to-menu（不应发生）

**Stub 方案**: MemberSim/Congestion/Satisfaction/Economy 用最小 serialize/deserialize stub（推进 RNG 状态 + 递增计数），标记为 Core 层集成点

**Edge cases**: 中间系统 validate 失败、网格数据冲突、引用不存在的 equipment_instance_id

**Estimated test count**: ~30 assertions

### SL-003 — Round-Trip Determinism and Resume-Paused Enforcement (Integration)
**Test file path**: `tests/integration/save_load/roundtrip_determinism_test.gd`
**What to test**:
- save→load→run→save2 与直接续跑 → save2 字节一致
- 三条件：(a) RNG 流状态精确恢复 (b) tick 顺序一致 (c) Navigation AStarGrid2D tie-break 位一致（ADR-0007 门禁已 PASSED）
- 加载后强制 paused（无论存档 speed）— Core Rule 7

**Stub 方案**: 全部 6 协调系统 stub 覆盖；Navigation rebuild 用 ADR-0007 已验证路径

**Edge cases**: 长时间运行后存档、多个 RNG 子流、paused 存档 round-trip

**Estimated test count**: ~25 assertions + 确定性 probe

### SL-004 — File I/O, JSON Encoding, and Version Checking (Integration)
**Test file path**: `tests/integration/save_load/file_io_version_test.gd`
**What to test**:
- AC-FILE-1: 每个 FileAccess.store_*() 返回值检查 — mock store_string() 返回 false → save_to_file() 报错
- AC-FILE-2: flush() 先于 close()（写后立即读文件内容验证）
- AC-FILE-3: 截断/损坏 JSON → "save file corrupted or truncated" 错误，无崩溃/部分解析
- AC-FILE-4: save_to_file("my_save") → `user_data_dir/saves/my_save.sav.json` 存在且 JSON 可解析
- 版本 exact-match：不匹配 → 用户可见"不兼容存档"消息，无迁移

**Edge cases**: 文件已存在覆盖、目录不存在、版本字段缺失

**Estimated test count**: ~30 assertions

---

## Manual QA Checklist

所有 8 张 story 均为 Logic/Integration，自动化测试完全覆盖。无强制手动 QA 项。
可选手动 sanity：垂直切片 demo 确认 tick 循环正常（可选，非阻塞）。

---

## Smoke Test Scope

1. Godot headless 启动不崩溃
2. TimeSystem tick 循环确定性（同 seed 同 tick 序列 → 位一致）
3. Save→Load round-trip 无数据丢失
4. 加载后自动暂停（不自动恢复运行）

---

## Playtest Requirements

*No playtest sessions required for this sprint*（纯逻辑/集成 story，无可玩 UI）。

---

## Definition of Done — This Sprint

- [ ] 8/8 story Complete，测试文件存在于指定路径
- [ ] 全量测试 ≥1290 全绿（1040 → 预计 +250）
- [ ] Smoke check PASS（`/smoke-check sprint`）
- [ ] QA sign-off APPROVED（`/team-qa sprint`）
- [ ] 无 S1/S2 bug
- [ ] Code reviewed and merged
- [ ] Foundation 层 4 epic 全绿 → 解锁 Core 层（placement-system + navigation）
