# GA-001/002 — 基础修复验证

日期：2026-09-06。用户授权顺序：基础修复 → 首日流程 → 美术样板。

## 已验证

- 冷会话加载保留精确器械ID，瑜伽垫／单车不因同形状混淆；朝向、等级、移动、取消、出售价格已覆盖。
- 存档预检拒绝未知／不一致身份；旧记录仅在目录几何匹配唯一时恢复身份。
- Satisfaction的新存档保留使用开始时的拥堵输入，读取历史值而非当下拥堵。
- SaveLoad通过临时文件写入、flush、关闭、替换更新磁盘存档。最终替换失败保护由单独回归补充。
- 主场景共享SeededRNG接线、中文保存／读档按钮、暂停保存、恢复暂停、清除拖放／购买状态、显示成功和失败信息；读档后显式刷新HUD。
- 必要JSON在构造场景前预检，缺失资源返回非零退出码，不产生冒烟PASS。

## 执行记录

全量runner：6204通过／0失败，`/tmp/ga-m0-suite3.log`，无SCRIPT ERROR／Parse Error。包含冷恢复22项回归与主场景／缺资源子进程检查。随后主场景补HUD刷新断言，独立探针15项／0失败（`/tmp/gym-ga002-main-probe-final.log`）。最终整体验证会在首日集成后重跑，以上不是新玩法验收。

真实主场景探针：`tests/integration/save_load/main_save_entry_probe.gd`。共享runner通过`main_entry_process_test.gd`运行独立SceneTree，避免生命周期未触发而误报通过。

## 限制

实际引擎窗口渲染：[暂停读档后的主场景](gym-foundation-restored.png)。使用真实Godot viewport输出，未重绘或替换UI内容。余额恢复为500，显示“读档成功／已暂停”；这是原沙盒基础接入，不是社区故事或最终美术样板。视觉检查发现透明文字压住背景、保存反馈偏小，首日UI接入时需使用独立高对比承载区。

退出时仍有原项目ObjectDB／资源未释放告警；不将其描述为已解决。旧进行中存档若缺历史输入，需明确拒绝策略。美术和首日玩法不在本基础测试结论内。
